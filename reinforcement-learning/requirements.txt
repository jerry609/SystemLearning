# 用于强化学习模块的Python依赖

# 核心RL环境库
gymnasium>=0.28.1

# 用于gymnasium环境渲染
pygame>=2.1.2

# LLM 与 PPO 训练核心库
torch>=2.0.0
transformers>=4.30.0
trl>=0.7.0
accelerate>=0.20.0
datasets>=2.14.0

# 数据处理和可视化
numpy>=1.24.0
matplotlib>=3.7.0
pandas>=2.0.0

# 可选：加速和优化
tokenizers>=0.13.0 