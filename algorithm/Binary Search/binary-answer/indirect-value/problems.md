# §2.3 二分间接值 - 题目列表

## 题目列表

| 题号 | 题目 | 难度 | 难度分 | 说明 |
|------|------|------|--------|------|
| 3143 | [正方形中的最多点数](https://leetcode.cn/problems/maximum-points-inside-the-square/) | 🟠 中等 | 1697 | 二分边长 |
| 1648 | [销售价值减少的颜色球](https://leetcode.cn/problems/sell-diminishing-valued-colored-balls/) | 🟠 中等 | 2050 | 二分最终高度 |

## 题目详解

### ⭐ 3143. 正方形中的最多点数

**题意**: 给定一些带标签的点和一个中心正方形，求最大的正方形边长，使得正方形内每个标签最多出现一次。

**核心思路**:
```python
def check(side_length):
    # 判断边长为 side_length 的正方形是否满足条件
    # 统计正方形内的点，检查标签是否重复
    labels = set()
    for x, y, tag in points:
        if abs(x) <= side_length and abs(y) <= side_length:
            if tag in labels:
                return False
            labels.add(tag)
    return True

# 二分边长，但答案需要通过边长计算点数
```

**关键点**:
- 二分的是边长（间接值）
- 答案是满足条件的点数（需要统计）
- 边长越大，包含的点越多

### ⭐ 1648. 销售价值减少的颜色球

**题意**: 有若干颜色的球，每种颜色有一定数量，价值递减，求卖出 orders 个球的最大价值。

**核心思路**:
```python
def check(final_height):
    # 判断降低到 final_height 能卖出多少球
    count = 0
    for num in inventory:
        if num > final_height:
            count += num - final_height
    return count <= orders

# 二分最终高度（间接值）
# 然后根据高度计算总价值（答案）
```

**详细过程**:
1. 二分找到最终高度 h
2. 所有 > h 的球降到 h
3. 剩余的 orders 从高度 h 继续取
4. 用等差数列求和计算总价值

**关键点**:
- 二分的是最终高度（间接值）
- 答案是总价值（需要计算等差数列和）
- 高度越低，卖出的球越多

**计算价值**:
```python
# 找到最终高度 h 后
total_value = 0
remaining = orders

for num in inventory:
    if num > h:
        # 从 num 降到 h，卖出 (num - h) 个
        # 价值为等差数列: h+1, h+2, ..., num
        count = num - h
        total_value += (h + 1 + num) * count // 2
        remaining -= count

# 剩余的 remaining 个球都是高度 h
total_value += h * remaining
```

## 二分间接值的一般步骤

### 步骤1: 识别间接值

分析题目，找到一个容易二分的量：
- 不一定是最终答案
- 但和答案有明确关系
- 容易验证其可行性

### 步骤2: 写 check 函数

基于间接值写检查函数：
```python
def check(indirect_value):
    # 判断这个间接值是否可行
    # 可能需要额外的计算
    return is_valid(indirect_value)
```

### 步骤3: 计算最终答案

```python
# 二分得到间接值
result = binary_search()

# 根据间接值计算最终答案
answer = calculate_answer(result)
```

## 对比常规二分答案

### 常规二分答案
```python
def check(answer):
    return is_achievable(answer)

# 二分得到答案
answer = binary_search()
return answer  # 直接返回
```

### 二分间接值
```python
def check(indirect):
    return is_valid(indirect)

# 二分得到间接值
indirect = binary_search()

# 还需要额外计算
answer = calculate_from_indirect(indirect)
return answer
```

## 识别技巧

什么时候考虑二分间接值？

1. **答案难以直接验证**
   - 直接二分答案的 check 函数不好写
   
2. **存在明确的中间变量**
   - 题目中有一个容易验证的量
   
3. **答案是计算得出的**
   - 最终答案需要通过某个值计算出来
   - 比如等差数列求和

## 练习建议

1. **对比常规二分**：理解为什么不直接二分答案
2. **分析关系**：间接值和答案的数学关系
3. **写好 check**：基于间接值的检查函数
4. **计算答案**：从间接值推导最终答案

## 常见模式

### 模式1: 二分高度/水平线

```python
# 二分一个高度/分界线
def check(height):
    # 统计高于/低于这个高度的数量
    count = count_above(height)
    return count <= limit

# 然后根据高度计算答案
```

### 模式2: 二分边界值

```python
# 二分一个边界值
def check(boundary):
    # 判断这个边界是否合法
    return is_valid_boundary(boundary)

# 根据边界计算最终结果
```

---

**返回**: [二分间接值](README.md) | [二分答案](../README.md)
