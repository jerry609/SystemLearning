# §2.4 最小化最大值 - 题目列表

> 用一个盖子（上界）去压住最大值，看看能否压住

## 题目列表

| 题号 | 题目 | 难度 | 难度分 | 说明 |
|------|------|------|--------|------|
| 410 | [分割数组的最大值](https://leetcode.cn/problems/split-array-largest-sum/) | 🔴 困难 | - | 经典题 |
| 2064 | [分配给商店的最多商品的最小值](https://leetcode.cn/problems/minimized-maximum-of-products-distributed-to-any-store/) | 🟠 中等 | 1886 | 最小化最大分配 |
| 3613 | [最小化连通分量的最大成本](https://leetcode.cn/problems/minimize-maximum-cost-of-connected-components/) | 🟠 中等 | ~1900 | 图+二分 |
| 1760 | [袋子里最少数目的球](https://leetcode.cn/problems/minimum-limit-of-balls-in-a-bag/) | 🟠 中等 | 1940 | 最小化最大球数 |
| 1631 | [最小体力消耗路径](https://leetcode.cn/problems/path-with-minimum-effort/) | 🟠 中等 | 1948 | 最小化路径最大高度差 |
| 2439 | [最小化数组中的最大值](https://leetcode.cn/problems/minimize-maximum-of-array/) | 🟠 中等 | 1965 | 通过操作最小化最大值 |
| 2560 | [打家劫舍 IV](https://leetcode.cn/problems/house-robber-iv/) | 🟠 中等 | 2081 | 最小化最大窃取金额 |
| 778 | [水位上升的泳池中游泳](https://leetcode.cn/problems/swim-in-rising-water/) | 🔴 困难 | 2097 | 最小化路径最大值 |
| 2616 | [最小化数对的最大差值](https://leetcode.cn/problems/minimize-the-maximum-difference-of-pairs/) | 🟠 中等 | 2155 | 最小化数对最大差 |
| 3419 | [图的最大边权的最小值](https://leetcode.cn/problems/minimize-maximum-edge-weight/) | 🔴 困难 | 2243 | 最小化最大边权 |
| 2513 | [最小化两个数组中的最大值](https://leetcode.cn/problems/minimize-the-maximum-of-two-arrays/) | 🟠 中等 | 2302 | 双数组最小化 |
| 3733 | [完成所有送货任务的最少时间](https://leetcode.cn/problems/minimum-time-to-complete-all-delivery-tasks/) | 🔴 困难 | - | **同2513题** |
| 3399 | [字符相同的最短子字符串 II](https://leetcode.cn/problems/smallest-substring-with-identical-characters-ii/) | 🔴 困难 | 2376 | 最小化最大长度 |
| 3605 | [数组的最小稳定性因子](https://leetcode.cn/problems/minimum-stability-of-an-array/) | 🔴 困难 | 2410 | 最小化最大频率 |
| - | [LCP 12. 小张刷题计划](https://leetcode.cn/problems/xiao-zhang-shua-ti-ji-hua/) | 🟠 中等 | - | 最小化最大难度 |
| 774 | [最小化去加油站的最大距离](https://leetcode.cn/problems/minimize-max-distance-to-gas-station/)🔒 | 🔴 困难 | - | **会员题** |

## 重点题目详解

### ⭐ 410. 分割数组的最大值

**题意**: 把数组分成 k 个连续子数组，最小化这些子数组和的最大值。

**核心思路**:
```python
def check(max_sum):
    # 判断能否分成 k 段，使得每段和 <= max_sum
    count = 1  # 至少一段
    current_sum = 0
    
    for num in nums:
        if current_sum + num > max_sum:
            count += 1
            current_sum = num
        else:
            current_sum += num
    
    return count <= k

# 二分最大和：[max(nums), sum(nums)]
```

**关键点**:
- 贪心：尽量让每段和接近但不超过 max_sum
- 盖子越高，需要的段数越少

### ⭐ 1760. 袋子里最少数目的球

**题意**: 最多操作 maxOperations 次，每次可以把一个袋子的球分成两个袋子，最小化单个袋子球数的最大值。

**核心思路**:
```python
def check(max_balls):
    # 判断能否通过操作让每个袋子的球数 <= max_balls
    operations = 0
    for balls in nums:
        if balls > max_balls:
            # 需要 (balls - 1) // max_balls 次操作
            operations += (balls - 1) // max_balls
    return operations <= maxOperations

# 二分最大球数：[1, max(nums)]
```

**关键点**:
- 球数为 x 的袋子分成 max_balls：需要 `(x-1) // max_balls` 次
- 盖子越高，需要的操作越少

### ⭐ 1631. 最小体力消耗路径

**题意**: 从左上角到右下角，路径的体力消耗是路径上相邻格子高度差的最大值，求最小体力消耗。

**核心思路**:
```python
def check(max_diff):
    # BFS/DFS：判断是否存在路径，使得最大高度差 <= max_diff
    visited = set()
    queue = [(0, 0)]
    visited.add((0, 0))
    
    while queue:
        x, y = queue.pop(0)
        if x == m-1 and y == n-1:
            return True
        
        for dx, dy in [(0,1), (1,0), (0,-1), (-1,0)]:
            nx, ny = x+dx, y+dy
            if 0 <= nx < m and 0 <= ny < n and (nx,ny) not in visited:
                if abs(heights[nx][ny] - heights[x][y]) <= max_diff:
                    visited.add((nx, ny))
                    queue.append((nx, ny))
    
    return False

# 二分最大高度差：[0, max高度差]
```

**关键点**:
- 二分最大高度差（盖子）
- BFS/DFS 验证路径是否存在

### ⭐ 2560. 打家劫舍 IV

**题意**: 至少偷 k 间房子，最小化偷窃金额的最大值。

**核心思路**:
```python
def check(max_amount):
    # 贪心：能偷的就偷，看能偷多少间（金额 <= max_amount）
    count = 0
    i = 0
    while i < len(nums):
        if nums[i] <= max_amount:
            count += 1
            i += 2  # 跳过下一间（不能相邻）
        else:
            i += 1
    return count >= k

# 二分最大金额：[min可能, max(nums)]
```

**关键点**:
- 盖子越高，能偷的房子越多
- 贪心策略：能偷就偷

### ⭐ 2616. 最小化数对的最大差值

**题意**: 选 p 个数对，最小化数对差值的最大值。

**核心思路**:
```python
def check(max_diff):
    # 排序后贪心：相邻配对，差值 <= max_diff
    nums.sort()
    count = 0
    i = 0
    while i < len(nums) - 1:
        if nums[i+1] - nums[i] <= max_diff:
            count += 1
            i += 2  # 配对成功，跳过两个
        else:
            i += 1
    return count >= p

# 二分最大差值：[0, max(nums) - min(nums)]
```

**关键点**:
- 先排序
- 贪心配对相邻元素

## 题目分类

### 1. 分割问题（3题）

将数组分割，最小化分割后的最大和/值：
- 410, 2064, 3399

**核心**: 贪心地尽量让每段接近但不超过上界

### 2. 路径问题（3题）

最小化路径中的最大边权/高度差：
- 1631, 778, 3419

**核心**: BFS/DFS 验证路径存在性

### 3. 操作优化（3题）

通过有限次操作，最小化某个最大值：
- 1760, 2439, 3605

**核心**: 计算达到上界需要的操作次数

### 4. 选择问题（2题）

选择若干元素/数对，最小化最大值：
- 2560, 2616

**核心**: 排序后贪心选择

### 5. 双数组问题（2题）

处理两个数组，最小化最大值：
- 2513, 3733

## Check 函数模式

### 模式1: 贪心分段

```python
def check(upper):
    # 贪心地分段，每段不超过 upper
    segments = 1
    current = 0
    for item in items:
        if current + item > upper:
            segments += 1
            current = item
        else:
            current += item
    return segments <= k
```

### 模式2: BFS/DFS 验证路径

```python
def check(upper):
    # 只走边权 <= upper 的边
    # BFS/DFS 验证能否到达终点
    return bfs_with_limit(upper)
```

### 模式3: 计算操作次数

```python
def check(upper):
    # 计算降到 upper 需要的操作次数
    operations = 0
    for item in items:
        if item > upper:
            operations += (item - 1) // upper
    return operations <= max_operations
```

### 模式4: 贪心选择

```python
def check(upper):
    # 贪心地选择满足条件的元素
    count = 0
    for i in range(len(items)):
        if condition(items[i], upper):
            count += 1
            # 可能需要跳过一些元素
    return count >= target
```

## 常见技巧

### 技巧1: 贪心策略

大多数 check 函数需要贪心：
- 尽量让每段/每份接近上界
- 能选就选（满足条件的情况下）

### 技巧2: 排序预处理

很多题目需要先排序：
```python
nums.sort()
# 然后二分答案
```

### 技巧3: 计算操作次数

```python
# 把 x 降到 limit 需要的操作
operations = (x - 1) // limit

# 把 x 分成若干个 <= limit 的数
parts = (x + limit - 1) // limit
```

## 练习建议

1. **理解"盖子"思想**：上界越高越容易满足
2. **掌握贪心策略**：check 函数通常需要贪心
3. **注意二分边界**：下界通常是问题的必要条件
4. **总结 check 模式**：分段、路径、操作等

## 常见错误

1. ❌ check 函数写错，贪心策略不对
2. ❌ 二分下界设置不合理（比如忘记考虑单个元素的最大值）
3. ❌ 操作次数计算错误
4. ❌ 路径验证时忘记标记已访问

---

**返回**: [最小化最大值](README.md) | [二分答案](../README.md)
