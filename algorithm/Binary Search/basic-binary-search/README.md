# 基础二分查找

## 核心概念

### 红蓝染色法

二分查找的本质是在有序数组中找到**红蓝边界**。

- **红色区域**：不满足条件的元素
- **蓝色区域**：满足条件的元素

### lowerBound 函数

`lowerBound(nums, x)` 返回 `≥x` 的第一个元素的下标。

设 `nums` 为递增（非递减）数组，长为 `n`。

## 查找需求表

### 查找元素位置

| 需求 | 写法 | 如果不存在 |
|------|------|-----------|
| ≥x 的第一个元素的下标 | `lowerBound(nums, x)` | 结果为 `n` |
| >x 的第一个元素的下标 | `lowerBound(nums, x+1)` | 结果为 `n` |
| <x 的最后一个元素的下标 | `lowerBound(nums, x) - 1` | 结果为 `-1` |
| ≤x 的最后一个元素的下标 | `lowerBound(nums, x+1) - 1` | 结果为 `-1` |

### 统计元素个数

| 需求 | 写法 |
|------|------|
| <x 的元素个数 | `lowerBound(nums, x)` |
| ≤x 的元素个数 | `lowerBound(nums, x+1)` |
| ≥x 的元素个数 | `n - lowerBound(nums, x)` |
| >x 的元素个数 | `n - lowerBound(nums, x+1)` |

**注意**: `<x` 和 `≥x` 互为补集，二者之和为 `n`。`≤x` 和 `>x` 同理。

## 模板代码

提供四种语言的模板实现：
- `template.py` - Python 实现
- `template.go` - Go 实现
- `template.java` - Java 实现
- `template.cpp` - C++ 实现

## 学习建议

1. **先理解红蓝染色法**：这是二分查找的核心思想
2. **熟记 lowerBound**：它是解决各种查找问题的基础
3. **练习基础题目**：从简单的二分查找开始
4. **掌握变体**：学会如何用 lowerBound 解决各种查找需求
5. **进阶练习**：在排序后的数组上进行二分查找

## 推荐阅读

- LeetCode 34 题解中的答疑
- LeetCode 35 题解中的答疑
- [二分查找 红蓝染色法【基础算法精讲 04】](视频链接)

---

**返回**: [二分查找专题](../README.md)
