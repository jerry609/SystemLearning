# §0.2 枚举中间题目列表

## 📝 核心题目

### 1. [2909. 元素和最小的山形三元组 II](https://leetcode.cn/problems/minimum-sum-of-mountain-triplets-ii/)
**难度**: 中等  
**题目难度**: 1479  
**标签**: 山形三元组  
**描述**: 找到满足 nums[i] < nums[j] > nums[k] 且 i < j < k 的三元组，使和最小  
**重要性**: ⭐⭐⭐⭐ 枚举中间的经典应用  

**解法**:
```python
def minimumSum(nums: List[int]) -> int:
    n = len(nums)
    
    # 预处理：left_min[j] 表示 nums[0..j-1] 的最小值
    left_min = [float('inf')] * n
    for j in range(1, n):
        left_min[j] = min(left_min[j-1], nums[j-1])
    
    # 预处理：right_min[j] 表示 nums[j+1..n-1] 的最小值
    right_min = [float('inf')] * n
    for j in range(n - 2, -1, -1):
        right_min[j] = min(right_min[j+1], nums[j+1])
    
    # 枚举中间 j
    min_sum = float('inf')
    for j in range(1, n - 1):
        if left_min[j] < nums[j] and nums[j] > right_min[j]:
            min_sum = min(min_sum, left_min[j] + nums[j] + right_min[j])
    
    return min_sum if min_sum != float('inf') else -1
```

---

### 2. [3583. 统计特殊三元组](https://leetcode.cn/problems/count-the-number-of-good-pairs/)
**难度**: 简单  
**题目难度**: 1510  
**标签**: 三元组计数  
**描述**: 统计特殊三元组的数量  
**提示**: 也可以用一次遍历  

---

### 3. [1930. 长度为 3 的不同回文子序列](https://leetcode.cn/problems/unique-length-3-palindromic-subsequences/)
**难度**: 中等  
**题目难度**: 1533  
**标签**: 回文子序列  
**描述**: 统计形如 "aba" 的回文子序列数量  
**重要性**: ⭐⭐⭐⭐ 经典题  

**解法思路**:
```python
def countPalindromicSubsequence(s: str) -> int:
    # 枚举中间字符 b
    # 统计每种字符在 b 左侧和右侧是否都出现过
    result = set()
    
    for b in range(len(s)):
        # 统计左侧出现的字符
        left = set(s[:b])
        # 统计右侧出现的字符
        right = set(s[b+1:])
        # 交集即为可以形成回文的字符
        for c in left & right:
            result.add((c, s[b], c))
    
    return len(result)
```

**优化**: 预处理每个字符第一次和最后一次出现的位置

---

### 4. [3128. 直角三角形](https://leetcode.cn/problems/right-triangles/)
**难度**: 中等  
**题目难度**: 1541  
**标签**: 二维枚举  
**描述**: 统计矩阵中直角三角形的数量  

**解法思路**:
```python
# 枚举直角顶点 (i, j)
# 统计同行和同列的 1 的数量
for i in range(m):
    for j in range(n):
        if grid[i][j] == 1:
            row_count = 同行其他 1 的数量
            col_count = 同列其他 1 的数量
            count += row_count * col_count
```

---

### 5. [2874. 有序三元组中的最大值 II](https://leetcode.cn/problems/maximum-value-of-an-ordered-triplet-ii/)
**难度**: 中等  
**题目难度**: 1583  
**标签**: 维护最值  
**描述**: 找到 (nums[i] - nums[j]) * nums[k] 的最大值，i < j < k  
**提示**: 也可以用一次遍历（枚举 k，维护左侧的 nums[i] - nums[j] 最大值）  

---

### 6. [447. 回旋镖的数量](https://leetcode.cn/problems/number-of-boomerangs/)
**难度**: 中等  
**标签**: 回旋镖模式  
**描述**: 统计满足到中心点距离相等的三元组 (i, j, k)  
**重要性**: ⭐⭐⭐⭐ 回旋镖模式的经典题  

**解法**:
```python
def numberOfBoomerangs(points: List[List[int]]) -> int:
    def distance(p1, p2):
        return (p1[0] - p2[0]) ** 2 + (p1[1] - p2[1]) ** 2
    
    count = 0
    # 枚举中心点
    for center in points:
        dist_count = {}
        # 统计其他点到中心的距离
        for point in points:
            if point == center:
                continue
            d = distance(center, point)
            dist_count[d] = dist_count.get(d, 0) + 1
        
        # 对于每个距离，选择两个点的排列数
        for cnt in dist_count.values():
            count += cnt * (cnt - 1)  # A(cnt, 2) = cnt * (cnt - 1)
    
    return count
```

---

### 7. [456. 132 模式](https://leetcode.cn/problems/132-pattern/)
**难度**: 中等  
**标签**: 单调栈 + 枚举中间  
**描述**: 找到满足 nums[i] < nums[k] < nums[j] 且 i < j < k 的三元组  
**提示**: 可以用单调栈，也可以枚举 j（中间）  

---

### 8. [3067. 在带权树网络中统计可连接服务器对数目](https://leetcode.cn/problems/count-pairs-of-connectable-servers-in-a-weighted-tree-network/)
**难度**: 中等  
**题目难度**: 1909  
**标签**: 树 + 枚举中间节点  
**描述**: 统计树中可连接的服务器对数  

---

### 9. [1534. 统计好三元组](https://leetcode.cn/problems/count-good-triplets/)
**难度**: 简单  
**标签**: 三元组计数  
**描述**: 统计满足差值约束的三元组  
**要求**: 做到 O(n²)  

---

### 10. [3455. 最短匹配子字符串](https://leetcode.cn/problems/find-the-shortest-superstring/)
**难度**: 困难  
**题目难度**: 2303  
**标签**: 复杂约束  
**描述**: 找到最短匹配子字符串  

---

### 11. [2242. 节点序列的最大得分](https://leetcode.cn/problems/maximum-score-of-a-node-sequence/)
**难度**: 困难  
**题目难度**: 2304  
**标签**: 图 + 四元组  
**描述**: 在图中找到得分最大的四节点序列  
**提示**: 枚举中间的边  

---

### 12. [2867. 统计树中的合法路径数目](https://leetcode.cn/problems/count-valid-paths-in-a-tree/)
**难度**: 困难  
**题目难度**: 2428  
**标签**: 树 + 质数  
**描述**: 统计路径上质数节点恰好为 1 的路径数  

---

### 13. [2552. 统计上升四元组](https://leetcode.cn/problems/count-increasing-quadruplets/)
**难度**: 困难  
**题目难度**: 2433  
**标签**: 四元组  
**描述**: 统计满足 nums[i] < nums[k] < nums[j] < nums[l] 且 i < j < k < l 的四元组  
**重要性**: ⭐⭐⭐⭐⭐ 非常经典，做法不止一种  

**解法思路**:
- 枚举中间两个位置 j 和 k
- 预处理左侧和右侧的信息
- 利用组合计数

---

### 14. [3257. 放三个车的价值之和最大 II](https://leetcode.cn/problems/maximum-value-sum-by-placing-three-rooks-ii/)
**难度**: 困难  
**题目难度**: 2553  
**标签**: 棋盘 + 枚举  
**描述**: 在棋盘上放三个车使价值和最大  

---

### 15. [3073. 最大递增三元组（会员题）](https://leetcode.cn/problems/maximum-increasing-triplet-value/)
**难度**: 中等  
**标签**: 递增三元组  
**会员题**  
**描述**: 找到最大递增三元组的值  

---

## 📊 学习进度

### 必做经典题
- [ ] 2909. 元素和最小的山形三元组 II ⭐⭐⭐⭐
- [ ] 1930. 长度为 3 的不同回文子序列 ⭐⭐⭐⭐
- [ ] 447. 回旋镖的数量 ⭐⭐⭐⭐
- [ ] 2552. 统计上升四元组 ⭐⭐⭐⭐⭐

### 三元组问题
- [ ] 2909. 元素和最小的山形三元组 II
- [ ] 3583. 统计特殊三元组
- [ ] 1930. 长度为 3 的不同回文子序列
- [ ] 3128. 直角三角形
- [ ] 2874. 有序三元组中的最大值 II
- [ ] 456. 132 模式
- [ ] 1534. 统计好三元组
- [ ] 3073. 最大递增三元组（会员题）

### 回旋镖模式
- [ ] 447. 回旋镖的数量

### 树和图问题
- [ ] 3067. 在带权树网络中统计可连接服务器对数目
- [ ] 2242. 节点序列的最大得分
- [ ] 2867. 统计树中的合法路径数目

### 四元组问题
- [ ] 2552. 统计上升四元组
- [ ] 2242. 节点序列的最大得分

### 困难题
- [ ] 3455. 最短匹配子字符串
- [ ] 3257. 放三个车的价值之和最大 II

## 💡 题目分类与技巧

### 类型1: 山形三元组
**特征**: nums[i] < nums[j] > nums[k]

**模板**:
```python
# 预处理左右最小值
left_min[j] = min(nums[0..j-1])
right_min[j] = min(nums[j+1..n-1])

# 枚举中间
for j in range(1, n-1):
    if left_min[j] < nums[j] > right_min[j]:
        process(left_min[j], nums[j], right_min[j])
```

### 类型2: 回文子序列
**特征**: a...b...a 形式

**模板**:
```python
# 枚举中间字符 b
for b in range(len(s)):
    left_chars = set(s[:b])
    right_chars = set(s[b+1:])
    # 左右都出现的字符可以形成回文
    common = left_chars & right_chars
```

### 类型3: 回旋镖（中心枚举）
**特征**: 到中心距离相等

**模板**:
```python
for center in points:
    dist_groups = {}
    for point in points:
        if point != center:
            d = distance(center, point)
            dist_groups[d] = dist_groups.get(d, 0) + 1
    
    # 从每组选 2 个的排列数
    for count in dist_groups.values():
        result += count * (count - 1)
```

### 类型4: 四元组优化
**特征**: i < j < k < l

**思路**:
- 枚举中间两个 j, k
- 预处理左右信息
- 利用组合计数

## 🎯 解题技巧

### 技巧1: 预处理左右信息
```python
# 避免重复计算
left[j] = preprocess_left(nums, j)
right[j] = preprocess_right(nums, j)

for j in range(1, n-1):
    result += calculate(left[j], nums[j], right[j])
```

### 技巧2: 按特征分组
```python
# 对于回旋镖类问题
groups = {}
for item in items:
    feature = extract_feature(center, item)
    groups[feature] = groups.get(feature, 0) + 1

# 计算组合数
for count in groups.values():
    result += C(count, 2)  # 或 A(count, 2)
```

### 技巧3: 组合计数
```python
# 从 n 个中选 k 个的组合数
def C(n, k):
    return math.comb(n, k)

# 从 n 个中选 k 个的排列数
def A(n, k):
    return math.perm(n, k)

# 快速计算
A(n, 2) = n * (n - 1)
C(n, 2) = n * (n - 1) // 2
```

### 技巧4: 左右组合
```python
# 左侧 a 种选择，右侧 b 种选择
# 总共 a × b 种组合
left_count = count_left(nums, j)
right_count = count_right(nums, j)
result += left_count * right_count
```

## 🔍 常见错误

1. **边界处理**: 枚举中间时注意范围 `range(1, n-1)`
2. **重复计数**: 注意是排列还是组合
3. **预处理错误**: left[j] 应该统计 j 左侧，不包括 j
4. **组合数公式**: C(n,2) 是 `n*(n-1)//2`，A(n,2) 是 `n*(n-1)`

## 🔗 相关资源

- 回到 [§0.2 枚举中间](README.md)
- 回到 [枚举专题主页](../README.md)
- 继续学习 [§0.3 遍历对角线](../diagonal-traversal/README.md)

## 📚 推荐学习顺序

| 顺序 | 题目 | 难度 | 重要性 | 备注 |
|------|------|------|--------|------|
| 1 | 2909 | 中等 | ⭐⭐⭐⭐ | 山形三元组入门 |
| 2 | 1930 | 中等 | ⭐⭐⭐⭐ | 回文子序列 |
| 3 | 3128 | 中等 | ⭐⭐⭐ | 二维枚举 |
| 4 | 447 | 中等 | ⭐⭐⭐⭐ | 回旋镖模式 |
| 5 | 1534 | 简单 | ⭐⭐⭐ | 优化练习 |
| 6 | 2552 | 困难 | ⭐⭐⭐⭐⭐ | 四元组挑战 |
