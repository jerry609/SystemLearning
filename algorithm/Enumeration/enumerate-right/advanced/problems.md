# §0.1.2 进阶题目

## 📝 进阶题目列表

### 1. [1010. 总持续时间可被 60 整除的歌曲](https://leetcode.cn/problems/pairs-of-songs-with-total-durations-divisible-by-60/)
**难度**: 中等  
**标签**: 模运算 + 两数之和  
**描述**: 统计时长之和可被 60 整除的歌曲对数  
**重要性**: ⭐⭐⭐⭐ 经典的模运算问题  

**一次遍历解法**:
```python
def numPairsDivisibleBy60(time: List[int]) -> int:
    count = 0
    freq = [0] * 60  # 余数范围 0-59
    for t in time:
        # 需要找的余数
        need = (60 - t % 60) % 60
        count += freq[need]
        freq[t % 60] += 1
    return count
```

**关键点**:
- `(60 - t % 60) % 60` 处理余数为 0 的情况
- 用数组代替哈希表优化性能

---

### 2. [3185. 构成整天的下标对数目 II](https://leetcode.cn/problems/count-pairs-that-form-a-complete-day-ii/)
**难度**: 中等  
**标签**: 模运算  
**描述**: 同 1010 题，时长之和可被 24 整除  
**提示**: 直接套用 1010 题的解法，改成模 24  

---

### 3. [2748. 美丽下标对的数目](https://leetcode.cn/problems/number-of-beautiful-pairs/)
**难度**: 简单  
**标签**: 特殊条件  
**描述**: 统计首位数字和末位数字互质的数对  

---

### 4. [2506. 统计相似字符串对的数目](https://leetcode.cn/problems/count-pairs-of-similar-strings/)
**难度**: 简单  
**标签**: 特征哈希  
**描述**: 统计包含相同字符集合的字符串对数  

**一次遍历解法**:
```python
def similarPairs(words: List[str]) -> int:
    count = 0
    freq = {}
    for word in words:
        # 用字符集合的哈希作为特征
        key = frozenset(word)  # 或转换为排序后的字符串
        count += freq.get(key, 0)
        freq[key] = freq.get(key, 0) + 1
    return count
```

---

### 5. [2874. 有序三元组中的最大值 II](https://leetcode.cn/problems/maximum-value-of-an-ordered-triplet-ii/)
**难度**: 中等  
**题目难度**: 1583  
**标签**: 维护最值  
**描述**: 找到满足 i < j < k 的三元组，使 (nums[i] - nums[j]) * nums[k] 最大  
**提示**: 也可以用一次遍历，枚举 k，维护左侧的 nums[i] - nums[j] 最大值  

---

### 6. [1031. 两个无重叠子数组的最大和](https://leetcode.cn/problems/maximum-sum-of-two-non-overlapping-subarrays/)
**难度**: 中等  
**题目难度**: 约 2000  
**标签**: 滑动窗口 + 维护最值  
**描述**: 找到两个不重叠子数组的最大和  

---

### 7. [2555. 两个线段获得的最多奖品](https://leetcode.cn/problems/maximize-win-from-two-segments/)
**难度**: 中等  
**题目难度**: 2081  
**标签**: 滑动窗口 + 动态规划  
**描述**: 用两个固定长度的线段覆盖最多的点  

---

### 8. [1995. 统计特殊四元组](https://leetcode.cn/problems/count-special-quadruplets/)
**难度**: 简单  
**标签**: 四个数  
**描述**: 统计满足 nums[a] + nums[b] + nums[c] == nums[d] 的四元组  

**优化思路**:
```python
# 枚举 c 和 d，维护左侧的 nums[a] + nums[b]
def countQuadruplets(nums: List[int]) -> int:
    n = len(nums)
    count = 0
    freq = {}
    
    for c in range(n - 1, 1, -1):
        # 更新 d 的位置
        for d in range(c + 1, n):
            freq[nums[d]] = freq.get(nums[d], 0) + 1
        
        # 枚举 a 和 b
        for a in range(c):
            for b in range(a + 1, c):
                target = nums[a] + nums[b] + nums[c]
                count += freq.get(target, 0)
    
    return count
```

---

### 9. [3404. 统计特殊子序列的数目](https://leetcode.cn/problems/count-special-subsequences/)
**难度**: 困难  
**题目难度**: 2445  
**标签**: 四个数 + 动态规划  
**描述**: 统计满足特殊条件的子序列  

---

### 10. [3267. 统计近似相等数对 II](https://leetcode.cn/problems/count-almost-equal-pairs-ii/)
**难度**: 困难  
**题目难度**: 2545  
**标签**: 字符串处理  
**描述**: 统计近似相等的数对  

---

### 11. [3480. 删除一个冲突对后最大子数组数目](https://leetcode.cn/problems/maximum-number-of-non-overlapping-palindrome-substrings/)
**难度**: 困难  
**题目难度**: 2764  
**标签**: 复杂约束  
**描述**: 删除一个冲突对后的最大子数组数目  

---

### 12. [1214. 查找两棵二叉搜索树之和](https://leetcode.cn/problems/two-sum-bsts/)
**难度**: 中等  
**标签**: 两数之和 + 树  
**会员题**  
**描述**: 在两棵 BST 中查找和为目标值的节点对  

---

### 13. [2964. 可被整除的三元组数量](https://leetcode.cn/problems/number-of-divisible-triplet-sums/)
**难度**: 中等  
**标签**: 三个数  
**会员题**  
**描述**: 统计可被整除的三元组  

---

## 🌟 思维扩展题目

### 14. [454. 四数相加 II](https://leetcode.cn/problems/4sum-ii/)
**难度**: 中等  
**标签**: 分组 + 哈希表  
**描述**: 从四个数组中各选一个数，使其和为 0  

**分组思路**:
```python
def fourSumCount(nums1, nums2, nums3, nums4):
    # 分成两组，每组两个数组
    # 第一组：计算 nums1[i] + nums2[j] 的所有可能
    sum_ab = {}
    for a in nums1:
        for b in nums2:
            sum_ab[a + b] = sum_ab.get(a + b, 0) + 1
    
    # 第二组：枚举 nums3[k] + nums4[l]，查找 -sum
    count = 0
    for c in nums3:
        for d in nums4:
            count += sum_ab.get(-(c + d), 0)
    
    return count
```

---

### 15. [3027. 人员站位的方案数 II](https://leetcode.cn/problems/find-the-number-of-ways-to-place-people-ii/)
**难度**: 困难  
**题目难度**: 2020  
**标签**: 几何 + 枚举  
**描述**: 计算人员站位的方案数  

---

### 16. [3548. 等和矩阵分割 II](https://leetcode.cn/problems/count-ways-to-split-array/)
**难度**: 困难  
**题目难度**: 2245  
**标签**: 前缀和 + 代码复用  
**描述**: 统计等和矩阵分割的方案数  

---

### 17. [3713. 最长的平衡子串 I](https://leetcode.cn/problems/find-longest-awesome-substring/)
**难度**: 困难  
**标签**: 状态压缩  
**描述**: 找到最长的平衡子串  
**提示**: 非暴力写法，使用状态压缩 + 哈希表  

---

## 📊 学习进度

### 必做题目
- [ ] 1010. 总持续时间可被 60 整除的歌曲 ⭐⭐⭐⭐
- [ ] 454. 四数相加 II ⭐⭐⭐⭐

### 模运算专题
- [ ] 1010. 总持续时间可被 60 整除的歌曲
- [ ] 3185. 构成整天的下标对数目 II

### 特殊条件
- [ ] 2748. 美丽下标对的数目
- [ ] 2506. 统计相似字符串对的数目

### 多元组问题
- [ ] 2874. 有序三元组中的最大值 II
- [ ] 1995. 统计特殊四元组
- [ ] 3404. 统计特殊子序列的数目
- [ ] 2964. 可被整除的三元组数量（会员题）
- [ ] 454. 四数相加 II

### 复杂问题
- [ ] 1031. 两个无重叠子数组的最大和
- [ ] 2555. 两个线段获得的最多奖品
- [ ] 3267. 统计近似相等数对 II
- [ ] 3480. 删除一个冲突对后最大子数组数目

### 扩展应用
- [ ] 1214. 查找两棵二叉搜索树之和（会员题）
- [ ] 3027. 人员站位的方案数 II
- [ ] 3548. 等和矩阵分割 II
- [ ] 3713. 最长的平衡子串 I

## 💡 进阶技巧

### 技巧1: 模运算优化
```python
# 对于求和可被 k 整除的问题
freq = [0] * k
for x in nums:
    need = (k - x % k) % k  # 注意处理 x % k == 0 的情况
    count += freq[need]
    freq[x % k] += 1
```

### 技巧2: 分组处理（四数问题）
```python
# 将 4 个数组分成两组，降低复杂度
# 从 O(n^4) 降到 O(n^2)
group1 = {}  # 第一组的所有和
for a in nums1:
    for b in nums2:
        group1[a + b] = group1.get(a + b, 0) + 1

count = 0
for c in nums3:
    for d in nums4:
        count += group1.get(-(c + d), 0)
```

### 技巧3: 特征提取
```python
# 将复杂对象转换为可哈希的特征
# 例如：字符集合 → frozenset
# 例如：比例 → 最简分数
from math import gcd

def get_ratio(w, h):
    g = gcd(w, h)
    return (w // g, h // g)

freq = {}
for w, h in rectangles:
    key = get_ratio(w, h)
    count += freq.get(key, 0)
    freq[key] = freq.get(key, 0) + 1
```

### 技巧4: 枚举中间元素
```python
# 对于需要维护两侧信息的问题
# 可以先从右向左预处理，再从左向右枚举
right_info = preprocess_from_right(nums)
left_info = initial_state()

for mid in range(n):
    # 利用两侧信息
    result = calculate(left_info, right_info[mid])
    # 更新左侧信息
    left_info = update(left_info, nums[mid])
```

## 🎯 学习建议

1. **从模运算开始**: 1010 是经典题，掌握模运算在枚举中的应用
2. **理解分组思想**: 454 题展示了如何通过分组降低复杂度
3. **多元组问题**: 三元组、四元组问题可以枚举中间，或分组处理
4. **特征提取**: 学会将复杂条件转换为简单的哈希键
5. **代码复用**: 相似问题可以复用解法（如 1010 和 3185）

## 🔗 相关资源

- 回到 [§0.1 枚举右，维护左](../README.md)
- 回到 [基础题目](../basics/problems.md)
- 继续学习 [§0.2 枚举中间](../../enumerate-middle/README.md)
