# §0.1.1 基础题目

## 📝 核心题目

### 1. [1. 两数之和](https://leetcode.cn/problems/two-sum/)
**难度**: 简单  
**标签**: 枚举右维护左 - 经典题  
**描述**: 找到数组中两个数之和等于目标值  
**重要性**: ⭐⭐⭐⭐⭐ 必做题，枚举右维护左的最经典应用  

**一次遍历解法**:
```python
def twoSum(nums: List[int], target: int) -> List[int]:
    seen = {}
    for j, x in enumerate(nums):
        if target - x in seen:
            return [seen[target - x], j]
        seen[x] = j
```

---

### 2. [2441. 与对应负数同时存在的最大正整数](https://leetcode.cn/problems/largest-positive-integer-that-exists-with-its-negative/)
**难度**: 简单  
**标签**: 两数之和变体  
**描述**: 找到数组中同时存在 x 和 -x 的最大正整数 x  
**提示**: 相当于两数之和等于 0  

**一次遍历解法**:
```python
def findMaxK(nums: List[int]) -> int:
    seen = set()
    max_k = -1
    for x in nums:
        if -x in seen:
            max_k = max(max_k, abs(x))
        seen.add(x)
    return max_k
```

---

### 3. [1512. 好数对的数目](https://leetcode.cn/problems/number-of-good-pairs/)
**难度**: 简单  
**标签**: 统计数对  
**描述**: 统计满足 nums[i] == nums[j] 且 i < j 的数对个数  
**提示**: 相当于两数之差等于 0  

**一次遍历解法**:
```python
def numIdenticalPairs(nums: List[int]) -> int:
    count = 0
    freq = {}
    for x in nums:
        count += freq.get(x, 0)  # 左侧有多少个 x
        freq[x] = freq.get(x, 0) + 1
    return count
```

---

### 4. [2001. 可互换矩形的组数](https://leetcode.cn/problems/number-of-pairs-of-interchangeable-rectangles/)
**难度**: 中等  
**题目难度**: 1436  
**标签**: 比例计数  
**描述**: 统计宽高比相同的矩形对数  

---

### 5. [1128. 等价多米诺骨牌对的数量](https://leetcode.cn/problems/number-of-equivalent-domino-pairs/)
**难度**: 简单  
**题目难度**: 1333  
**标签**: 统计等价对  
**描述**: 统计等价的多米诺骨牌对数  

---

### 6. [121. 买卖股票的最佳时机](https://leetcode.cn/problems/best-time-to-buy-and-sell-stock/)
**难度**: 简单  
**标签**: 维护最小值  
**描述**: 找到买卖股票的最大利润  
**重要性**: ⭐⭐⭐⭐⭐ 必做题，维护左侧最小值  

**一次遍历解法**:
```python
def maxProfit(prices: List[int]) -> int:
    min_price = float('inf')
    max_profit = 0
    for price in prices:
        max_profit = max(max_profit, price - min_price)
        min_price = min(min_price, price)
    return max_profit
```

---

### 7. [2016. 增量元素之间的最大差值](https://leetcode.cn/problems/maximum-difference-between-increasing-elements/)
**难度**: 简单  
**标签**: 维护最小值  
**描述**: 找到满足 i < j 且 nums[i] < nums[j] 的最大差值  
**提示**: 类似 121 题  

---

### 8. [219. 存在重复元素 II](https://leetcode.cn/problems/contains-duplicate-ii/)
**难度**: 简单  
**标签**: 下标约束  
**描述**: 判断是否存在两个相同元素，且下标之差不超过 k  

**一次遍历解法**:
```python
def containsNearbyDuplicate(nums: List[int], k: int) -> bool:
    seen = {}
    for j, x in enumerate(nums):
        if x in seen and j - seen[x] <= k:
            return True
        seen[x] = j
    return False
```

---

### 9. [2260. 必须拿起的最小连续卡牌数](https://leetcode.cn/problems/minimum-consecutive-cards-to-pick-up/)
**难度**: 中等  
**题目难度**: 1365  
**标签**: 最小窗口  
**描述**: 找到包含至少一对相同数字的最小连续子数组长度  

---

### 10. [2815. 数组中的最大数对和](https://leetcode.cn/problems/max-pair-sum-in-an-array/)
**难度**: 简单  
**标签**: 按特征分组  
**描述**: 找到数位最大值相同的两个数的最大和  

---

### 11. [2342. 数位和相等数对的最大和](https://leetcode.cn/problems/max-sum-of-a-pair-with-equal-sum-of-digits/)
**难度**: 中等  
**标签**: 按特征分组  
**描述**: 找到数位和相同的两个数的最大和  

**一次遍历解法**:
```python
def maximumSum(nums: List[int]) -> int:
    max_sum = -1
    max_by_digit_sum = {}
    for x in nums:
        digit_sum = sum(int(d) for d in str(x))
        if digit_sum in max_by_digit_sum:
            max_sum = max(max_sum, x + max_by_digit_sum[digit_sum])
        max_by_digit_sum[digit_sum] = max(max_by_digit_sum.get(digit_sum, 0), x)
    return max_sum
```

---

### 12. [1679. K 和数对的最大数目](https://leetcode.cn/problems/max-number-of-k-sum-pairs/)
**难度**: 中等  
**标签**: 两数之和变体  
**描述**: 找到和为 k 的数对最多有多少个  

---

### 13. [面试题 16.24. 数对和](https://leetcode.cn/problems/pairs-with-sum-lcci/)
**难度**: 中等  
**标签**: 两数之和  
**描述**: 找到所有和为目标值的数对  

---

### 14. [3623. 统计梯形的数目 I](https://leetcode.cn/problems/count-the-number-of-inversions/)
**难度**: 中等  
**题目难度**: 1580  
**标签**: 组合计数  
**描述**: 统计梯形的数目  

---

### 15. [3371. 识别数组中的最大异常值](https://leetcode.cn/problems/identify-the-largest-outlier-in-an-array/)
**难度**: 中等  
**题目难度**: 1644  
**标签**: 特殊条件查找  
**描述**: 识别数组中的最大异常值  

---

### 16. [624. 数组列表中的最大距离](https://leetcode.cn/problems/maximum-distance-in-arrays/)
**难度**: 中等  
**标签**: 维护最值  
**描述**: 在多个有序数组中找最大距离  

---

### 17. [2364. 统计坏数对的数目](https://leetcode.cn/problems/count-number-of-bad-pairs/)
**难度**: 中等  
**题目难度**: 1622  
**标签**: 转换条件  
**描述**: 统计不满足 j - i != nums[j] - nums[i] 的数对  
**提示**: 转换为 nums[j] - j != nums[i] - i  

---

### 18. [1014. 最佳观光组合](https://leetcode.cn/problems/best-sightseeing-pair/)
**难度**: 中等  
**题目难度**: 1730  
**标签**: 维护最值  
**描述**: 找到最佳观光组合，最大化 values[i] + values[j] + i - j  
**重要性**: ⭐⭐⭐⭐ 经典题  

**一次遍历解法**:
```python
def maxScoreSightseeingPair(values: List[int]) -> int:
    max_score = 0
    max_i = values[0]  # 维护 values[i] + i 的最大值
    for j in range(1, len(values)):
        max_score = max(max_score, max_i + values[j] - j)
        max_i = max(max_i, values[j] + j)
    return max_score
```

---

### 19. [1814. 统计一个数组中好对子的数目](https://leetcode.cn/problems/count-nice-pairs-in-an-array/)
**难度**: 中等  
**题目难度**: 1738  
**标签**: 转换条件  
**描述**: 统计满足 nums[i] + rev(nums[j]) == nums[j] + rev(nums[i]) 的数对  
**提示**: 转换为 nums[i] - rev(nums[i]) == nums[j] - rev(nums[j])  

---

### 20. [3584. 子序列首尾元素的最大乘积](https://leetcode.cn/problems/find-the-key-of-the-numbers/)
**难度**: 中等  
**题目难度**: 1763  
**标签**: 维护最值  
**描述**: 找到子序列首尾元素乘积的最大值  

---

### 21. [2905. 找出满足差值条件的下标 II](https://leetcode.cn/problems/find-indices-with-index-and-value-difference-ii/)
**难度**: 中等  
**题目难度**: 1764  
**标签**: 下标和值双重约束  
**描述**: 找到满足下标差和值差条件的下标对  
**思考题**: 改成求个数要怎么做？  

---

## 📊 学习进度

### 必做经典题
- [ ] 1. 两数之和 ⭐⭐⭐⭐⭐
- [ ] 121. 买卖股票的最佳时机 ⭐⭐⭐⭐⭐
- [ ] 1014. 最佳观光组合 ⭐⭐⭐⭐

### 简单题（入门）
- [ ] 2441. 与对应负数同时存在的最大正整数
- [ ] 1512. 好数对的数目
- [ ] 2016. 增量元素之间的最大差值
- [ ] 219. 存在重复元素 II

### 中等题（提升）
- [ ] 2001. 可互换矩形的组数
- [ ] 1128. 等价多米诺骨牌对的数量
- [ ] 2260. 必须拿起的最小连续卡牌数
- [ ] 2815. 数组中的最大数对和
- [ ] 2342. 数位和相等数对的最大和
- [ ] 1679. K 和数对的最大数目
- [ ] 面试题 16.24. 数对和
- [ ] 3623. 统计梯形的数目 I
- [ ] 3371. 识别数组中的最大异常值
- [ ] 624. 数组列表中的最大距离
- [ ] 2364. 统计坏数对的数目
- [ ] 1814. 统计一个数组中好对子的数目
- [ ] 3584. 子序列首尾元素的最大乘积
- [ ] 2905. 找出满足差值条件的下标 II

## 💡 题目分类

### 类型1: 两数之和/差
- 1. 两数之和
- 2441. 与对应负数同时存在的最大正整数
- 1679. K 和数对的最大数目
- 面试题 16.24. 数对和

### 类型2: 统计相同/等价对
- 1512. 好数对的数目
- 2001. 可互换矩形的组数
- 1128. 等价多米诺骨牌对的数量
- 219. 存在重复元素 II
- 2260. 必须拿起的最小连续卡牌数

### 类型3: 维护左侧最值
- 121. 买卖股票的最佳时机
- 2016. 增量元素之间的最大差值
- 1014. 最佳观光组合

### 类型4: 按特征分组
- 2815. 数组中的最大数对和
- 2342. 数位和相等数对的最大和

### 类型5: 转换条件
- 2364. 统计坏数对的数目
- 1814. 统计一个数组中好对子的数目

## 🎯 解题技巧总结

### 技巧1: 两数之和模板
```python
seen = {}
for j, x in enumerate(nums):
    if target - x in seen:
        # 找到答案
        ...
    seen[x] = j
```

### 技巧2: 统计数对模板
```python
count = 0
freq = {}
for x in nums:
    count += freq.get(x, 0)  # 左侧有多少个满足条件的
    freq[x] = freq.get(x, 0) + 1
```

### 技巧3: 维护最值模板
```python
max_val = float('-inf')
left_max = ...
for x in nums:
    max_val = max(max_val, 计算当前值)
    left_max = max(left_max, x)  # 更新左侧最值
```

### 技巧4: 条件转换
```python
# 原条件: nums[i] + rev(nums[j]) == nums[j] + rev(nums[i])
# 转换为: nums[i] - rev(nums[i]) == nums[j] - rev(nums[j])

freq = {}
for x in nums:
    key = x - rev(x)  # 转换后的特征
    count += freq.get(key, 0)
    freq[key] = freq.get(key, 0) + 1
```

## 🔍 常见错误

1. **自己和自己配对**: 记得先查找再更新
2. **重复计数**: 注意去重逻辑
3. **下标越界**: 检查边界条件
4. **溢出问题**: Python 不用担心，其他语言注意

## 🔗 相关资源

- 回到 [§0.1 枚举右，维护左](../README.md)
- 继续学习 [§0.1.2 进阶题目](../advanced/README.md)
- 回到 [枚举专题主页](../../README.md)
