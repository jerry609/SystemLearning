# §0.1.1 基础部分说明

## 📖 学习目标

掌握"枚举右，维护左"的基础应用，能够用一次遍历解决以下问题：
- 两数之和/差/积类问题
- 统计满足条件的数对个数
- 在约束条件下求最值
- 按特征分组处理

## 💡 核心模式

### 模式1: 查找型（Two Sum）
```python
# 找到满足条件的一对元素
seen = {}
for j, x in enumerate(nums):
    target_val = calculate_target(x)  # 计算需要的值
    if target_val in seen:
        return process_answer(seen[target_val], j)
    seen[x] = j
```

### 模式2: 计数型
```python
# 统计满足条件的数对个数
count = 0
freq = {}
for x in nums:
    # 左侧有多少个满足条件的元素
    count += freq.get(calculate_key(x), 0)
    # 更新频率
    freq[x] = freq.get(x, 0) + 1
return count
```

### 模式3: 最值型
```python
# 在满足条件下求最大/最小值
result = initial_value
left_info = ...  # 维护左侧信息
for j, x in enumerate(nums):
    # 利用左侧信息计算
    if left_info is valid:
        result = optimize(result, calculate(x, left_info))
    # 更新左侧信息
    left_info = update(left_info, x)
return result
```

## 🎓 学习路线

### 第一步：经典入门（必做）
1. **[1. 两数之和](https://leetcode.cn/problems/two-sum/)** ⭐⭐⭐⭐⭐
   - 理解枚举右维护左的核心思想
   - 掌握哈希表的使用
   - 注意先查找后更新

2. **[121. 买卖股票的最佳时机](https://leetcode.cn/problems/best-time-to-buy-and-sell-stock/)** ⭐⭐⭐⭐⭐
   - 学习维护左侧最小值
   - 理解如何在遍历中同时更新和使用信息

### 第二步：简单练习
3. **[1512. 好数对的数目](https://leetcode.cn/problems/number-of-good-pairs/)**
   - 学习计数模式
   - 理解如何统计左侧满足条件的元素个数

4. **[219. 存在重复元素 II](https://leetcode.cn/problems/contains-duplicate-ii/)**
   - 处理下标约束条件
   - 学习如何维护有效的哈希表

### 第三步：进阶应用
5. **[1014. 最佳观光组合](https://leetcode.cn/problems/best-sightseeing-pair/)** ⭐⭐⭐⭐
   - 综合应用维护最值和计算答案
   - 理解如何转换问题

6. **[2342. 数位和相等数对的最大和](https://leetcode.cn/problems/max-sum-of-a-pair-with-equal-sum-of-digits/)**
   - 学习按特征分组
   - 维护每组的最大值

7. **[1814. 统计一个数组中好对子的数目](https://leetcode.cn/problems/count-nice-pairs-in-an-array/)**
   - 学习条件转换技巧
   - 将复杂条件转换为简单的相等判断

## 📊 难度分布

- **简单**: 1, 2441, 1512, 2016, 219
- **中等**: 其余题目
- **推荐顺序**: 1 → 121 → 1512 → 219 → 1014 → 2342 → 1814

## 💭 重要提示

### 一次遍历的关键
1. **先使用后更新**: 避免元素和自己配对
2. **维护必要信息**: 不要维护过多无用信息
3. **及时更新**: 确保左侧信息是最新的

### 常见模式
- **查找**: `if target in seen`
- **计数**: `count += freq.get(key, 0)`
- **最值**: `max_val = max(max_val, current)`
- **更新**: `seen[x] = value`

## 🔗 下一步

完成基础题目后，继续学习 [§0.1.2 进阶题目](../advanced/README.md)
